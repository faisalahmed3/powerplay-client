import React from 'react';
import { Outlet } from 'react-router';

const ManageEvents = () => {
    return (
        <div>
            <Outlet></Outlet>
        </div>
    );
};

export default ManageEvents;